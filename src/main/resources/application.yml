server:
  shutdown: graceful
  port: 8080

spring:
  application:
    name: coc-layout-bot

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: never

logging:
  config: classpath:logback-spring.xml
